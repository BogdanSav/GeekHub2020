import React from 'react';
import { Typography} from "@material-ui/core"
function LoginTitle(){
   return (
      <Typography variant="h3">Sign in into your account</Typography>
   );
}

export default LoginTitle;